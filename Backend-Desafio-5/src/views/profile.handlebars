
<main>
  <h2 class="title">Perfil</h2>
  {{#if products.length}}
    {{#each products}}
      <ul>
        <li>Producto: {{name}}</li>
        <li>Descripción: {{description}}</li>
        <li>Precio: ${{price}}</li>
        <li>Stock: {{stock}}</li>
      </ul>
      <br />
    {{/each}}
  {{else}}
    <p class="no__products">No tenemos productos</p>
  {{/if}}
</main>